package com.revature.controllers;

// import com.revature.services.OwnerService;
// import org.springframework.beans.factory.annotation.Autowired;
// import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

@RestController
public class OwnerController {

    // @Autowired
    // private OwnerService ownerService;

    // Register a new owner
    // Param will be an Owner JSON
//    @PostMapping("/register")
//    public ResponseEntity<OwnerModel> registerOwner(@RequestBody OwnerModel owner) {
//        OwnerModel owner = ownerService.registerOwner(owner);
//        return ResponseEntity.ok(owner);
//    }

    // Get properties for a specific owner
    // Param should be a JWT token probably?
//    @GetMapping("/properties")
//    public ResponseEntity<List<PropertyModel>> getPropertiesForOwner() {
//        List<PropertyModel> properties = ownerService.getPropertiesForOwner();
//        return ResponseEntity.ok(properties);
//    }

}